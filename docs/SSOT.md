# SSOT 맵

이 프로젝트의 모든 "진실의 원본"이 어디에 사는지 기록하는 레지스트리.
코드를 작성하기 전에 이 표를 먼저 확인할 것. 여기 없는 원본을 새로 만들면 이 표에 추가할 것.
문서 갱신은 관련 코드 변경과 같은 커밋에 포함한다.

## 현재 유효한 원본

| 지식의 종류 | 원본 위치 | 파생물 | 비고 |
| --- | --- | --- | --- |
| 제품 스펙·확정 결정(D1~D13) | [에픽 이슈 #1](https://github.com/kaeba0616/songGalaxy/issues/1) | 하위 이슈 #2~#8 | 스펙 본문을 다른 문서에 복제하지 말 것 |
| 작업 단위·순서·완료 기준 | 이슈 #2~#8 | — | 각 이슈의 Acceptance Criteria가 완료 정의 |
| 개발 프로세스 규칙 | `.claude/skills/ssot/SKILL.md`, `.claude/skills/commit-with-prompts/SKILL.md` | — | |
| 배포·운영 환경 | Vercel 프로젝트 `song-galaxy` (https://song-galaxy.vercel.app) | — | 프로덕션 DB = Neon(Marketplace, env 자동 주입). 로컬 dev DB = Docker 5433 (.env). 환경별 DATABASE_URL로 분리. **주의: 스키마 변경 시 로컬·Neon 양쪽에 push할 것** (한쪽만 하면 프로덕션 API 500 — 2026-08-09 /api/stars 장애 사례) |
| DB 스키마 | `src/db/schema.ts` (Drizzle) | 마이그레이션(`drizzle-kit push`), TS 타입(`$inferSelect`) | SQL·타입 수기 중복 정의 금지 |
| 앱 상수 | `src/config/constants.ts` | — | `MIN_LIKES_FOR_STAR=5`, `BIG_THEME_COUNT=12`, `GALAXY_SONG_COUNT=30000` 등 |
| 환경변수 접근 | `.env` → `src/config/env.ts` | — | `process.env` 직접 접근 금지 (예외: `drizzle.config.ts`, 파일 내 주석 참조) |
| 유저 취향 원본 | `likes` 테이블 | `user_stars`(파생 캐시) | 무효화: 좋아요 변경 시 즉시 재계산 |
| 곡 데이터 원천(기본) | HuggingFace `maharshipandya/spotify-tracks-dataset` → `scripts/ingest-dataset.ts` | `songs` 테이블 (3만 곡, source=`spotify-tracks-114k`) | 멱등: `(source, source_id)` 유니크. 2022-10 스냅샷 |
| 곡 데이터 원천(즉석 편입) | 외부 iTunes Search API → `src/server/import-song.ts` | `songs` 테이블 (source=`itunes`, batch=`user-import`) | /songs 검색에서 유저 트리거. iTunes 장르 매핑: `genre-clusters.ts`의 `ITUNES_GENRE_TO_GENRE` |
| 곡 데이터 원천(주간 신곡) | 외부 MusicBrainz API → `src/server/new-releases.ts` | `songs` 테이블 (source=`musicbrainz`, batch=`mb-weekly-<날짜>`) | 최근 7일 공식 싱글 + 장르 태그 필수 + 상한. 롤백=배치 삭제. CLI(`npm run ingest:new`)와 Cron(`/api/cron/ingest-new-releases`, vercel.json, 매주 월 03:00 UTC, CRON_SECRET 보호)이 공용 |
| 곡 데이터 원천(카탈로그 보강) | 초기 데이터셋 CSV → `scripts/fill-artist-catalog.ts` | `songs` 테이블 (source=`spotify-tracks-114k`, batch=`catalog-fill-<날짜>`) | 은하에 있는 가수의 곡 중 인기도 컷오프(43)로 빠진 것을 채운다. 특징이 있어 이웃 배치가 정확. 같은 곡의 다른 track_id는 제거. 롤백=배치 삭제 |
| 곡 중복 정리 | `scripts/dedupe-songs.ts` | `songs` 테이블 | 가수·제목 정규화 키가 같으면 하나만 남긴다(인기도 → 특징 보유 → id 순). 부제는 떼지 않는다("Song"과 "Song - Live"는 다른 녹음일 수 있음). 좋아요가 걸린 곡은 지우지 않음. 삭제 전 `songs_backup_dedupe`로 백업 |
| 곡 데이터 원천(아티스트 확장) | 외부 Last.fm API → `src/server/artist-expand.ts` | `songs` 테이블 (source=`lastfm`, batch=`lastfm-expand-<날짜>`) | 은하에 있는 가수의 대표곡 중 없는 곡을 채운다. 장르는 그 가수의 기존 곡에서 상속(추정하지 않음), 인기도는 청취자 수 로그 스케일. `LASTFM_API_KEY` 없으면 아무 동작도 하지 않음. CLI: `npm run expand:artists`. **배치 ID가 날짜(UTC)뿐이라 같은 날 두 번 돌리면 한 배치로 합쳐진다 — 롤백 시 id 범위를 함께 걸 것**. 실행 후 `npm run audit:expand`로 동명이인 오귀속 점검 |
| 확장 배치 오귀속 점검 | `scripts/audit-expand.ts` | 사람이 보는 후보 목록 | Last.fm이 autocorrect로 이름을 바꿔 돌려준 가수를 찾는다(문장부호까지 비교). 실측: 'RAINBOW.'(french)에 영국 록밴드 Rainbow 곡 3개가 붙었다. 후보 9건 중 8건은 표기만 다른 동일 인물이었으므로 **자동 삭제 금지, 사람이 판단**. 철자가 완전히 같은 동명이인은 이 방법으로 못 잡음 |
| 이웃 기반 좌표 수학 | `src/lib/neighbor-place.ts` | `place-song.ts`(곡 단위)·`fill-artist-catalog.ts`(대량) 공용 | 특징 z-score → 최근접 8곡 거리 가중 평균 → 경계 클램프. 두 경로가 같은 규칙을 써야 하므로 여기만 수정 |
| 신규 곡 좌표 배치 | `src/server/place-song.ts` (`placeSong`) | 즉석 편입·주간 신곡·아티스트 확장이 공용 | ① 조회표에서 특징을 찾아 같은 장르 최근접 이웃 8곡의 좌표 중심 → ② 같은 가수 곡 근처 → ③ 장르 구역 랜덤 순으로 폴백. 성단/은하 경계 클램프 |
| 오디오 특징 조회표 | `dataset_features` 테이블 (`scripts/ingest-features.ts`가 CSV에서 적재) | 신규 곡 배치의 특징 원천 | 데이터셋 고유 8.9만 곡 전체(은하 미적재분 포함). `data/`는 배포되지 않으므로 DB에 둔다. **스키마 변경 시 로컬·Neon 양쪽 적재 필요** |
| 곡 매칭 키 정규화 | `src/lib/match-key.ts` | 조회표 적재·조회가 공용 | 규칙을 바꾸면 `dataset_features`를 다시 적재해야 함 |
| 오디오 특징 목록 | `src/config/constants.ts`의 `AUDIO_FEATURE_KEYS` | 적재·배치·이웃 찾기 | 순서까지 동일해야 함 |
| 좌표 수학 유틸 | `src/lib/layout-math.ts` | 배치 스크립트·즉석 편입이 공용 | scripts/lib에서 이동 |
| 장르→성단 매핑 | `src/config/genre-clusters.ts` | `themes` 테이블 (`scripts/build-themes.ts`가 생성) | 성단 12개·색상 포함. 매핑 변경은 신규 곡에만 적용 |
| 행성 테마 팔레트 | `src/config/planet-themes.ts` | `users.planet_theme`(slug 저장), 밤하늘 렌더링 | 꾸미기 맛보기(이슈 #10). 테마 추가는 여기서만 |
| 앨범아트·미리듣기 | 외부 iTunes Search API → 없으면 Deezer 공개 API (`src/server/enrich.ts`) | `songs.artwork_url/preview_url` (파생 캐시, `/api/enrich`) | `enriched_at`으로 조회 여부 기록. 두 곳 다 응답 실패 시에만 캐시 안 함(재시도). 아트도 미리듣기도 못 구한 캐시는 `EMPTY_RETRY_MS`(7일)가 지나면 다시 조회 — 빈 결과를 영구 캐시하면 안 됨(실측 324곡이 그렇게 굳었다가 재조회로 306곡 복구). 일괄 재조회 CLI: `npm run refresh:media`. 후보 3건을 받아 제목·가수 일치 점수로 고른다(둘 다 일치 요구 금지 — 스토어가 가수명을 현지어로 돌려줌). **URL만 저장하고 음원 파일은 보관하지 않는다** (양 API 조건) |
| 가수 정보 | 외부 MusicBrainz API (CC0) | `artists` 테이블 (파생 캐시, `src/server/artist-info.ts`) | 이름당 1회 조회. 검색 점수 85 미만은 버림(오매칭 방지) |
| 가사 | 외부 LRCLIB API | `songs.plain_lyrics/synced_lyrics` (파생 캐시, `src/server/lyrics.ts`) | 곡당 1회 조회. 출처 표기 필수 |
| 외부 데이터 출처 표기 | `src/components/DataCredits.tsx`의 `SOURCES` | `/credits` 페이지, /songs·/songs/[id]·/me 푸터, 은하 좌상단 한 줄 | 새 외부 소스를 붙이면 여기에만 추가한다. 페이지에 출처를 따로 적지 않는다. Last.fm은 API 약관이 출처 표기와 백링크를 요구 |
| YouTube 영상 ID | 외부 YouTube Data API | `songs.youtube_video_id` (파생 캐시, `src/server/youtube.ts`) | 곡당 1회 검색(쿼터 100곡/일). 키 없으면 검색 링크 폴백. **`getYoutubeVideoId(songId, userId)`를 부르는 곳은 두 군데뿐이다**(`userId`는 이 조회의 값을 치를 사람이다 — 한도 선점이 함수 안에서 일어난다): ① 목록에 곡을 담을 때(`addSongToPlaylist`) ② 소유자가 자기 목록 상세(`/lists/[id]`)를 열 때 아직 못 찾은 곡 최대 `YOUTUBE_LOOKUP_RETRY_MAX`곡(`retryMissingVideos`) — ②가 필요한 이유는 쿼터가 마른 날의 담기가 `youtube_checked_at`을 남기지 않아 그대로 두면 그 곡이 영영 미리듣기에 갇히기 때문. **공개 페이지(`/songs/[id]`, `/list/[slug]`)에서는 절대 부르지 않는다** — force-dynamic이라 크롤러가 링크만 따라와도 하루 쿼터가 통째로 마른다(캐시된 `songs.youtube_video_id`만 읽는다). 한 사람이 전체 쿼터를 말리지 못하도록 사용자별 하루 한도(`YOUTUBE_LOOKUPS_PER_USER_PER_DAY`)를 두며, 소비량은 `youtube_lookups` 테이블에 쌓는다(아래 행). 임베드 거부(100·101·150)를 만나면 `clearYoutubeVideoId`가 ID만 지우고 `youtube_checked_at`은 남긴다 — 재검색하면 같은 영상을 또 찾아오며 쿼터만 태운다. 창구는 `DELETE /api/songs/[id]/video`(로그인 필요 + 신고자의 목록 어딘가에 그 곡이 담겨 있어야 함(`songInUsersPlaylist`), 현재 캐시된 ID와 일치할 때만 삭제) |
| 곡 좌표 | `songs.pos_x/y/z` (`scripts/layout-songs.ts`가 1회 기록) | 렌더러 페이로드 | 기록 후 불변. NULL인 곡만 채움. 배치 수학: `scripts/lib/layout-math.ts` |
| 로그인·좋아요·내 별 클라이언트 상태 | `src/likes/likes-context.tsx` (`LikesProvider`) | `GalaxyCanvas`·`MiniPlayer`의 ♥ | 서버 원본은 `likes` 테이블, 창구는 `/api/likes`. 컴포넌트에서 `/api/likes` 직접 호출·중복 보관 금지 (예외: `LikeButton`은 서버가 넘긴 initialLiked로 동작 — 추후 통합 대상) |
| 미리듣기 재생 상태·미디어 클라이언트 캐시 | `src/player/player-context.tsx` (`PlayerProvider`) | `MiniPlayer`, `GalaxyCanvas` 재생 UI | 오디오 객체·큐·볼륨·`/api/enrich` 캐시의 단일 원본. 컴포넌트에서 `Audio` 직접 생성 금지. **"이 큐에서 다음에 무엇을 어떤 엔진으로 트나"는 `findPlayable` 한 곳에서만 정한다** — 자동 진행·이전/다음·목록 시작·영상 실패 폴백이 모두 이걸 쓴다(경로마다 따로 판단하면 "첫 곡 하나가 재생 불가면 목록 전체가 죽는" 구멍이 생긴다). 이미 듣고 있는 큐에서 곡을 고르는 UI는 `playInQueue`를 쓴다 — `playFrom`은 mode를 `browse`로 못 박으므로 목록 재생 중에 부르면 전곡 재생이 30초 미리듣기로 강등된다. 설계: `docs/superpowers/specs/2026-08-10-global-player-design.md` |
| 노래 목록 | `playlists`/`playlist_songs` 테이블 (`src/server/playlists.ts`가 유일한 접근 창구) | `/lists`, `/lists/[id]`, `/list/[slug]`, 알약 담기 팝오버 | 곡 개수는 저장하지 않고 `count(*)`로 구한다. 공유는 `share_slug` 하나로 표현 — NULL이면 비공개, 끄면 기존 링크가 죽는다. 편집은 `user_id` 소유자만. `findMyPlaylist`/`setPlaylistShared`는 "내 목록이 아님"과 "찾았음"을 타입으로 구분해 반환한다(`{ ok: false }` vs `{ ok: true; shareSlug }`) — 예전엔 둘 다 null을 반환해 PATCH가 실패를 성공으로 보고했었다. `addSongToPlaylist`는 `"added" \| "already" \| "forbidden" \| "nosong"`을 반환하며, `playlist_songs.song_id`에 FK가 없어 insert 전에 반드시 `songs` 존재를 확인한다. `/lists`로 가는 입구는 은하 드로어의 "≡ 내 노래 목록"과 알약 담기 팝오버 아래 링크 두 곳이다 — 입구를 없애면 만든 목록에 다시 닿을 길이 사라진다. `/lists`·`/lists/[id]`·`/list/[slug]`는 모두 `robots: noindex`(공유 링크는 "아는 사람만"이 전제) |
| YouTube 검색 쿼터 소비량 | `youtube_lookups` 테이블 (`(user_id, day)` PK, `src/server/youtube.ts`의 `reserveLookup`이 유일한 기록 지점) | `YOUTUBE_LOOKUPS_PER_USER_PER_DAY` 한도 판정 | **계산으로 되살릴 수 없어 저장하는 값이다**("계산할 수 있으면 저장하지 않는다"의 예외). "실제로 태운 검색 횟수"를 센다 — 캐시 적중·키 없음은 한 유닛도 쓰지 않으므로 세지 않고, 403·타임아웃으로 실패한 호출은 쿼터를 먹으므로 센다(되돌리지 않는다). 증가와 한도 검사는 UPSERT 한 문장(`on conflict do update ... where count < 한도`)으로 원자적이어야 한다 — "세고 나서 조회"로 나누면 동시 요청이 같은 잔량을 읽고 전부 통과한다. 예전엔 "내 목록 곡 중 24시간 내 `youtube_checked_at`이 찍힌 곡 수"로 셌는데, 곡을 빼면 카운터가 되돌아가고(담기→조회→빼기 무한 반복) 실패한 조회는 아예 세지 않았다. 날짜는 UTC(`(now() at time zone 'utc')::date`). 정리 작업은 두지 않는다 — 행이 사용자·일 단위로만 늘어난다 |
| 공유 링크 slug | `src/lib/share-slug.ts` | `playlists.share_slug` | 혼동 문자(0 O 1 l I) 제외한 32자 알파벳 10자리. 충돌은 DB unique 제약이 최종 방어 |
| 재생 엔진 선택 | `src/player/engine.ts`의 `pickEngine` | `PlayerProvider`의 엔진 전환 | 목록 재생 + 영상 ID가 있을 때만 `youtube`, 아니면 `preview`. **탐색 경로에서 YouTube를 쓰면 안 된다** (쿼터·UX) |
| YouTube 전곡 재생 | YouTube IFrame Player API (`src/player/YoutubeStage.tsx`가 그리고, `src/player/player-context.tsx`의 `PlayerProvider`가 제어) | 알약의 영상 패널 | **ytdl류 스트림 추출 금지** — 약관 위반 + 기술적 보호조치 우회. 영상은 화면에 보여야 하므로 **접기 = 일시정지**를 강제한다 — 이 강제는 `PlayerProvider`의 `setVideoExpanded` 안에 있다(UI 쪽 스타일링이 아니라 여기가 원본). 영상 실패(`onError`)는 그 곡을 건너뛰지 않고 **그 곡만 미리듣기로 떨어뜨린다**(`reportYoutubeError`) — 건너뛰면 임베드 막힌 영상 하나로 그 곡을 모든 목록에서 잃는다. `YT_ERROR_WINDOW_MS` 안에 오류가 `YT_ERROR_LIMIT`번 쌓이면 큐 전체를 `mode: "browse"`로 내리고 안내(`notice`)를 띄운다 — 오류→다음 곡→오류가 iframe 로드 속도로 목록 전체를 태우는 것을 막는다. 이 폭주 차단은 **시간 창**이라 창보다 넓게 흩어진 오류(재생 도중의 코드 5 등)에는 영영 걸리지 않으므로, **곡별 시도 상한(`YT_PLAYBACK_ERROR_LIMIT`)을 따로 둔다**: 같은 곡에서 `playback` 오류가 그만큼 쌓이면 그 곡의 영상 ID를 **큐에서만** 지워 미리듣기로 내리고 안내를 띄운다(서버 캐시는 건드리지 않는다 — 영상 자체는 멀쩡할 수 있다). 첫 오류는 같은 영상으로 한 번 더 시도한다 — 한 번 튄 것만으로 멀쩡한 영상을 버리면 안 된다. 두 장치는 서로를 대신하지 않으므로 어느 쪽도 지우지 말 것. **은하 카드 캐러셀은 `mode: "playlist"` 큐를 가져가지 않는다**(`GalaxyCanvas`의 진입 복원·행성 탈출 두 곳) — 캐러셀을 띄우면 `uiHosted`로 알약이 내려가면서 영상 무대가 무너져 전곡 재생이 미리듣기 목록으로 바뀐다. 설계: `docs/superpowers/specs/2026-08-11-playlists-youtube-design.md` |
| YouTube 무대 마운트·손잡이 등록 타이밍 | `src/player/YoutubeStage.tsx`(`videoId` prop, `onReady`) / `src/player/MiniPlayer.tsx`(`stageMounted` 조건) / `src/player/player-context.tsx`(대기 요청 1슬롯) | 알약의 영상 패널 | `YT.Player`를 `videoId` 없이 만들면 `onReady`가 영영 오지 않는다(빈 `/embed/` 문서라 플레이어 스크립트가 안 돎) → 제어 손잡이가 등록되지 않아 YouTube 재생이 조용히 영원히 실패한다. 그래서 `YoutubeStage`는 항상 실제 영상 ID로 생성하고, `registerYoutube`로 넘기는 손잡이는 반드시 `onReady` 이후에만 있다(그 전엔 `YT.Player`의 메서드가 없다). `MiniPlayer`가 무대를 거는 조건은 `(engine === "youtube" \|\| queue?.mode === "playlist") && stageVideoId !== null`이어야 한다 — "engine이 youtube일 때만"으로 좁히면 교착에 빠진다(엔진은 손잡이가 있어야 youtube가 되는데, 손잡이는 무대가 서야 생긴다). `PlayerProvider`는 무대가 없을 때 들어온 요청을 1개짜리 슬롯에 담아뒀다가 `onReady`에서 그대로 재생한다(`YT_STAGE_READY_TIMEOUT_MS`, `src/config/constants.ts`). 버려지는 요청은 항상 reject된다(`superseded`/`stopped`) — 호출부는 이 두 사유를 정상 동작으로 삼켜야 한다 |
| 테스트 러너 | `package.json`의 `npm test` (`tsx --test $(find src -name "*.test.ts")`) | `src/**/*.test.ts` | Node 20 내장 `node:test`를 `tsx`로 돌린다 — vitest 아님. Node 20 기본 테스트 파일 패턴이 `.ts`를 못 잡아 `find`로 파일을 직접 넘긴다. 이 환경은 `npm install`이 재현적으로 멈추므로 새 테스트 프레임워크를 설치로 추가하지 말 것 |
