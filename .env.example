# 로컬 개발 기본값. 실제 값은 .env에 (커밋 금지)
# 접근은 반드시 src/config/env.ts를 통해서만 (docs/SSOT.md)
DATABASE_URL=postgres://postgres:postgres@localhost:5433/songgalaxy
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
AUTH_SECRET=
